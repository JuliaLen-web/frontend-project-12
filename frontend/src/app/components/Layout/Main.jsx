const Main = ({ children }) => (
  <main className="container-fluid vh-100 pt-5">
    <div className="d-flex flex-column justify-content-center align-items-center h-100">
      {children}
    </div>
  </main>
)

export default Main
